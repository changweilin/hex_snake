const HexSnakeState = {
  audio: {
    muted: false,
    unlocked: false
  },
  config: {},
  game: {},
  replay: {},
  ui: {}
};

const HexSnakeUI = {};

window.HexSnakeState = HexSnakeState;
window.HexSnakeUI = HexSnakeUI;
